原子tool列表

1. 加载缓存的token+home信息(自动根据过期时间判断,如果需要刷新,则刷新,如果为空或已经过期,则返回空)
3. 更新绑定的home信息
2. 生成oauthurl
3. 根据code,获取真正的token,保存下来
4. get家庭信息,如果有多个
4. get房间信息,传入homeId,token,同步所有房间信息,跟老的房间信息进行比较(房间信息有来源,从小米同步),
5. get设备信息,传入homeId,token,同步所有设备信息,跟老的设备信息进行比较


大的组合tool:

1. 同步小米设备,则使用一个组合型skill
    1. 加载缓存的token
    2. 如果失败,则生成oauthurl
    3. 根据code,获取真正的token,保存下来
    4. 检查缓存homeId是否为空,如果为空则同步家庭信息,同步的信息超过2行,则询问用户需要绑定的家庭,绑定homeId
    5. 同步房间信息
    6. 同步设备信息

